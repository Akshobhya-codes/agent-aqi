# ── Execution mode ─────────────────────────────────────────────────────────────
# sim   = fully synthetic, no external keys needed
# quote = real Uniswap quotes via POST /quote + async /jobs  (requires UNISWAP_API_KEY)
# real  = sign + broadcast on-chain (Phase 2.3, requires AGENT_PRIVATE_KEY)
EXECUTION_MODE=sim               # sim | quote | real

# ── QuickNode Streams ──────────────────────────────────────────────────────────
QUICKNODE_STREAMS_WEBHOOK_SECRET=
QUICKNODE_STREAM_ID=
QUICKNODE_NETWORK=base-sepolia

# ── Uniswap ────────────────────────────────────────────────────────────────────
UNISWAP_API_KEY=
SWAP_SENDER_ADDRESS=

# ── Base chain ─────────────────────────────────────────────────────────────────
BASE_RPC_URL=https://sepolia.base.org
CHAIN_ID=84532
AGENT_PRIVATE_KEY=

# ── Blockade Labs Skybox ───────────────────────────────────────────────────────
BLOCKADE_LABS_API_KEY=
BLOCKADE_LABS_STYLE_ID=2

# ── x402 paywall ──────────────────────────────────────────────────────────────
X402_ENABLED=false
X402_DEMO_PROOF=
X402_RECEIVER_ADDRESS=
X402_AMOUNT=0.001 ETH

# ── EscrowPredictionPool on-chain ─────────────────────────────────────────────
PREDICTION_ENABLED=false
PREDICTION_CONTRACT_ADDRESS=
ADMIN_PRIVATE_KEY=
ADMIN_TOKEN=

# ── SIWE Auth + Profiles ──────────────────────────────────────────────────────
#
# REQUIRED: Set a strong random secret for signing JWTs.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Origin of the web app (used for CORS)
APP_ORIGIN=http://localhost:3000

# How long auth tokens stay valid (default: 24 hours)
AUTH_TOKEN_TTL_HOURS=24
